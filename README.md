1. Download the zip -> React-M5 folder
2. Open in vs code
3. Open terminal -> cmd
4. Use the command to change directory -> cd my-first-react-app
5. Command to install node_modules folder -> npm install or npm i
6. Use the command to run react app (my-first-react-app) -> npm run dev
